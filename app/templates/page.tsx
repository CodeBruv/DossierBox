/**
 * Placeholder pages — Block 1 only builds the shell.
 * This stub ensures the route resolves. The real Templates page
 * (document-family vs. visual-presentation distinction) is the
 * next implementation step.
 */
import { Container } from "@/ui";

export default function TemplatesPlaceholder() {
  return (
    <div className="margin-top-lg">
      <Container>
        <h1>Templates</h1>
        <p>Professional document families for every purpose. Placeholder page.</p>
      </Container>
    </div>
  );
}
